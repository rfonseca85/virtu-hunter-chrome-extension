Now I want to implement this new fields from the image. Make sure we add them to the options page (only if I alredy dont have a similar field, if so, try to use it to this case as well) Implement it on options page as well in the engine, classifier, autofill